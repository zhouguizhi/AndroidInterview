<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hztuen.androidinterview">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".handler.HandlerActivity"/>
        <activity android:name=".jdk7Objects.Jdk7ObjectsActivity"/>
        <activity android:name=".RequestMinValueActivity"/>
        <activity android:name=".reflect.ReflectTestActivity"/>
        <activity android:name=".activity.OneActivity"/>
        <activity android:name=".activity.TranActivity" />
    </application>

</manifest>